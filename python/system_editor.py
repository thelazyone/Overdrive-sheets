"""
Tk ship / systems editor — alias for :mod:`systems_editor`.

From the ``python/`` directory::

    python system_editor.py

Same program as ``python systems_editor.py`` (presets, slots, slot ``label``, library).
"""

from __future__ import annotations

from systems_editor import main

if __name__ == "__main__":
    main()
